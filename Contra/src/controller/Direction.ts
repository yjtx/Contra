module one {
    export const enum Direction {
        NONE,
        LEFT,
        RIGHT,
        UP,
        DOWN
    }
    
    export const enum BUTTONS {
        NONE,
        JUMP,
        ATTACK
    }
}